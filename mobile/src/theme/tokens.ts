/**
 * tokens.ts — Re-exporting all verified design tokens from theme.ts
 *
 * NOTE: theme.ts is the single source of truth (extracted verbatim from Google Stitch export).
 * All tokens (both kebab-case flat and PascalCase structured) are defined in theme.ts.
 */
export * from './theme';
